import React from 'react';

const LoadingSpinner = () => {
	return (
		<div className="spinner-container spinner-container_screen">
			<div className="spinner"/>
		</div>
	);
};

export default LoadingSpinner;